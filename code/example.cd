function functionName(paramNameA, paramNameB, paramNameC) {
  var variableName = 10;
  if (variableName == 50 && (true || variableName <= 6)) {
    while (condition) {
        return &someValue && [55.9576];
    }
  } else {
    return false || *somePointer;
  }
}
